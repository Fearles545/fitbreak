# Handoff: 2026-03-29 Session 1

## What was done

1. **Exercise documentation** — Created `docs/adding-rotation.md` and `docs/adding-strength-workout.md` as guides for adding new rotations and strength workouts via data entry.

2. **Second user onboarded (Yulia)** — Seeded exercises and templates for user `accd3a74-460b-4ee8-b5da-fcd105bc375c` (yuliapoddyacha@gmail.com):
   - 2 custom micro-break rotations: `knee-safe-active` (5 exercises), `knee-activation` (4 exercises)
   - Standard rotations 1-3 copied from Leo's account (neck-eyes, thoracic-shoulders, hips-lower-back)
   - 2 strength workouts: Квадрицепс (5 exercises), Сідниці і стабілізація (5 exercises)
   - 7 workout templates total
   - `user_settings` updated with custom `enabled_rotations` and `rotation_order` (no standard `active` rotation)

3. **DB migration applied** — `add_knee_rotation_keys`: added `knee-safe-active` and `knee-activation` to `micro_break_rotation` CHECK constraint.

4. **Code changes** — Updated `MicroBreakRotation` type, `ROTATION_ORDER`, and `ROTATION_INFO` with 2 new rotation keys. Updated `fitbreak-supabase-schema.sql` doc.

5. **Architecture exploration** — Deep analysis of how to make rotations per-user instead of hardcoded. Used Explore agent (17 touchpoints mapped), backend engineer (data model proposal), and product lead (marketplace analysis).

6. **Two decisions documented** — DECISION-015 (data-driven rotations) and DECISION-016 (difficulty toggle).

7. **Sprint 4 planned** — 4 tasks for per-user data architecture.

## Decisions made

- **DECISION-015:** Remove hardcoded `ROTATION_ORDER`, `ROTATION_INFO`, `MicroBreakRotation` type. Derive rotation metadata from `workout_templates`. Store template UUIDs in user_settings instead of string keys. Migrate historical BreakEntry JSONB.
- **DECISION-016:** Workout difficulty toggle (easy/medium/hard). `difficulty_overrides` JSONB per exercise, `last_difficulty` per template. Changeable mid-workout.

## Current state

- Sprint 4 planned but not started
- Yulia's exercises are in the DB and functional
- FE has hardcoded rotation keys (including 2 new ones) — works but fragile. Sprint 4 removes this.
- `rotationKeyFromTemplate()` in `break-timer.service.ts` is missing the 2 new rotation names in its name map — will be resolved by Sprint 4 task #2 which removes this method entirely
- Exercise source doc: `docs/yulia-exercises.md`

## Next steps

1. Start Sprint 4 — tasks #1 and #3 (DB migrations) can run in parallel
2. Then FE migration (#2) and difficulty toggle UI (#4)
3. Test Yulia's account to verify exercises load correctly

## Blockers or concerns

- `rotationKeyFromTemplate()` has a hardcoded name map missing the 2 new rotations. The fallback (sort_order position) may not work correctly for Yulia's rotations. Sprint 4 resolves this by removing the method, but until then Yulia's custom rotations may not display correctly in the break prompt options list.
